# Gradle Properties Configuration

# Project-specific properties
org.gradle.jvmargs=-Xmx2048m

# Define the Gradle build cache location
org.gradle.caching=true

# Enable parallel build
org.gradle.parallel=true
